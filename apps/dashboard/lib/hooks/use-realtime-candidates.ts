'use client'

// Supabase Realtime has been removed; this hook is a no-op until a replacement
// (e.g. polling or a different realtime provider) is wired up.
export function useRealtimeCandidates(_companyId: string | null) {
  // no-op
}
